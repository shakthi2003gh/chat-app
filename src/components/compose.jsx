const Compose = () => {
  return (
    <div className="compose-container">
      <div className="compose">
        <input type="text" />

        <button>send</button>
      </div>
    </div>
  );
};

export default Compose;
