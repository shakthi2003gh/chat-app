const Header = () => {
  return (
    <header>
      <div className="header">
        <div className="title">Chat app</div>

        <button>Log out</button>
      </div>
    </header>
  );
};

export default Header;
